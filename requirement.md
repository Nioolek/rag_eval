请你完整、全流程开发一个企业级大型 Python RAG 评测系统，严格遵循软件工程规范、设计模式与高性能编码标准，按照需求理解与澄清→架构设计→基类编写→功能模块开发→测试用例编写→完整系统测试的标准流程完成全部开发工作，最终输出可直接部署运行、生产级可用的完整项目代码。

项目技术栈约束

开发语言：Python

前端界面：Gradio 库（交互友好、无卡顿、流式渲染流畅）

RAG 接口调用：基于 LangGraph 的 RemoteGraph 调用外部 RAG 服务

大模型评测：基于 LangChain OpenAI 实现需要大模型参与的评测逻辑

数据存储：优先本地文件存储，兼容 SQLite 数据库，支持数据版本管理

强制设计模式要求

为保证系统高可扩展性、低耦合、易维护、易迭代，代码必须严格遵循以下设计模式：

策略模式：用于评测指标（Metric）的统一管理，不同指标独立实现

工厂模式：用于评测指标实例化、存储方式（本地 / SQLite）创建

适配器模式：用于适配不同 RAG 接口、不同数据格式

模板方法模式：统一标准化评测、标注、数据处理的核心流程

单例模式：用于全局配置、数据库连接、并发控制器等全局唯一组件

迭代器模式：用于标注数据、评测结果的遍历与读取

所有代码需遵循面向对象设计原则，模块化拆分清晰，符合大型项目工程化标准。

核心功能需求

一、标注功能模块

实现标注数据的增删改查全功能

标注字段设计具备高拓展性，支持后期快速新增字段

基础标注字段：

RAG 输入字段：用户 query、多轮对话历史 query、agentid、语言、是否开启思考

标注结果字段：gt 文档、FAQ 是否匹配、是否拒答、多标准答案、回答风格要求、备注、自定义拓展字段

标注数据支持本地存储，兼容 SQLite，支持版本管理

二、标注结果统计模块

实现标注数据的可视化统计，展示核心指标（如标注总数量、分类统计等基础指标）

三、评测功能模块

支持并发评测数据，并发数量在 Gradio 前端可配置

支持配置1 个或 2 个 RAG 评测接口，双接口可左右对比效果

内置Metric 基类，支持后续快速新增各类评测指标

支持历史评测结果回溯查看

必须完整实现以下所有 RAG 通用评测指标：

（1）检索相关性指标

检索精确率、检索召回率

MRR（平均倒数排名）、Hit Rate@k

检索片段与问题的相关性得分

（2）生成质量指标

答案事实一致性（无幻觉）

答案与问题的相关性

答案完整性、流畅度、可读性

拒答准确率（正确识别应该拒答 / 不拒答的场景）

幻觉检测评分

（3）FAQ 专属评测指标

FAQ 匹配准确率、FAQ 召回率

FAQ 答案与标准答案的一致性

（4）综合与附加指标

多标准答案匹配度

回答风格匹配度

多轮对话一致性

上下文利用率

答案重复率检测

四、评测结果展示模块

支持评测结果整体概览 + 单条详情查看

单条结果完整展示 RAG 返回的所有字段：query 改写结果、FAQ 匹配结果、检索结果、重排序结果、最终大模型输出等

支持单条评测结果重跑，重跑时前端流式实时展示 Markdown 格式内容

前端友好展示所有评测指标计算结果

异步处理规范（强制要求）

所有 I/O 密集型操作（文件读写、网络请求、数据库操作、RAG 接口调用、大模型请求）必须使用 async/await 异步实现

所有 CPU 密集型操作（指标计算、数据处理、文本解析）必须使用进程池执行

严格避免阻塞 Gradio 前端主线程，保证界面全程响应流畅

安全要求（强制要求）

所有 API Key、密钥、配置信息仅从环境变量读取，禁止硬编码

敏感信息（密钥、用户数据、评测结果）不记录到日志中

所有用户输入、文件上传、接口参数必须进行数据验证与清理

实现防护机制，防止路径遍历攻击，保障文件存储安全

性能优化要求（强制要求）

大文件（标注数据、评测日志）采用分块读取 / 写入处理

SQLite 数据库查询字段添加索引，提升查询效率

频繁访问的配置、统计数据使用内存缓存

Gradio 前端使用懒加载，优化渲染速度与内存占用

必须交付物（完整产出）

完整可运行的项目源代码

requirements.txt 依赖声明文件

README.md 详细使用说明文档

完整的项目安装步骤

项目配置说明（含环境变量、参数配置）

功能使用示例（标注 / 评测 / 结果查看）

系统 API 文档（模块、函数、接口说明）

.env.example 环境变量模板文件

完整单元测试用例 + 测试运行说明

验收标准（生产级要求）

所有功能模块（标注、统计、评测、展示）可独立 / 联动正常运行

单元测试覆盖率 100%，所有测试用例执行通过

Gradio 界面响应流畅，无卡顿、无崩溃

系统稳定支持至少 1000 条标注数据的存储与处理

高并发评测任务稳定运行，无内存泄漏、无任务丢失

异步 / 进程池调度正常，无线程阻塞问题

所有评测指标计算准确，结果展示正常

代码质量要求

代码结构清晰，严格遵守指定设计模式，模块化程度高

遵循 Python 编码规范，注释完整，可读性强

异常处理完善，兼容各类边界场景与错误情况

Metric 基类设计通用、简洁，新增评测指标无需修改核心代码

全程遵循异步、安全、性能优化规范，符合生产环境使用标准

